---
strand: structure
level: foundation
order: 7
title: Subgroups and Lagrange's Theorem
prerequisites:
  - tier: strand-2-structure-foundation
    slug: 06-symmetry-groups
    description: Symmetry groups
connections:
  - strand-2-structure-foundation/08-rings-and-fields
  - strand-1-number-quantity-advanced/00-fermat-and-euler-totient
applications:
  - cs: "Coset-based crypto, error-correcting codes"
  - life: "The structure inside structure — subsets that respect operations"
---

# Subgroups and Lagrange's Theorem

## Explain Like I Am 7

Inside a big toolbox of moves, you can sometimes find a *smaller*
toolbox that's complete on its own — every two of its moves still
combine inside, every move still has its undo.  That hidden mini-kit
is called a **subgroup**.  Lagrange spotted a magical rule about these
mini-kits: the size of a sub-kit always divides evenly into the size of
the big kit, like a small box of chocolates packing perfectly inside a
bigger box with nothing rattling around.  That rule alone solves a
huge pile of puzzles in number theory.

## Mental

A **subgroup** $H$ of a group $G$ is a subset of $G$ that is itself
a group under the same operation.

For $H \subseteq G$ to be a subgroup:

1. $e \in H$ (identity).
2. $a, b \in H \Rightarrow a \star b \in H$ (closed under operation).
3. $a \in H \Rightarrow a^{-1} \in H$ (closed under inverse).

Examples:

- $\{0, 4, 8, 12, \ldots\} = 4\mathbb{Z}$: a subgroup of $(\mathbb{Z},
  +)$.
- $\{[0], [3]\}$: subgroup of $(\mathbb{Z}/6\mathbb{Z}, +)$.
- $C_n \subset D_n$: rotations form a subgroup of dihedral.
- $A_n \subset S_n$: even permutations form a subgroup of symmetric.

## Lagrange's theorem

> If $H$ is a subgroup of finite group $G$, then $|H|$ **divides**
> $|G|$.

**Consequence**: a finite group $G$ of order $n$ has subgroups only
of orders dividing $n$. So $C_5$ (order 5) has subgroups of order
$1$ or $5$ only — no subgroups of order $2, 3, 4$.

This is one of the most useful theorems in group theory. Every time
you want to find a subgroup, the candidate orders are constrained
to divisors of $|G|$.

## Cosets

The proof of Lagrange uses **cosets**. For $H \le G$ and $a \in G$,
the **left coset** is $aH = \{a \star h : h \in H\}$.

All cosets have the same size $|H|$. They **partition** $G$. So $|G|
= [G : H] \cdot |H|$, where $[G : H]$ is the number of cosets — the
**index** of $H$ in $G$.

## Interactive

:::widget type=numeric-input prompt="$|G| = 12$. Possible subgroup orders: divisors of 12: $1, 2, 3, 4, 6, 12$. Number of distinct possible orders?" answer=6 explain="6 divisors of 12.":::

:::widget type=numeric-input prompt="$|G| = 7$ (prime). Subgroup orders can only be..." answer=0 explain="$1$ or $7$. Type 0 to indicate 'two values'. Prime-order groups have only trivial subgroups.":::

:::widget type=numeric-input prompt="$|D_4| = 8$. Subgroup of rotations $C_4$ has order $4$. The index $[D_4 : C_4] = 8/4 = ?$" answer=2 explain="Index 2.":::

:::widget type=numeric-input prompt="By Lagrange, can $S_5$ have a subgroup of order $7$? ($|S_5| = 120$. $7 \\nmid 120$. So no.)" answer=0 explain="No.":::

## Symbolic

**Lagrange's theorem**: for finite $G$ and subgroup $H$,

$$
|G| = [G : H] \cdot |H|.
$$

Therefore $|H| \mid |G|$.

**Order of element divides order of group**: for $a \in G$, the
order of $a$ (smallest $k$ with $a^k = e$) divides $|G|$. This is
because $\langle a \rangle$ — the cyclic subgroup generated by $a$
— has order equal to the order of $a$.

This is **why Fermat's little theorem works** (Strand 1 Advanced
Lesson 00): in $(\mathbb{Z}/p\mathbb{Z})^*$ of order $p-1$, every
element raised to $p-1$ gives $e = [1]$.

## Computational

```python
def is_subgroup(H, G_op, identity):
    """Test if a set H ⊆ G with operation G_op is a subgroup."""
    # Identity in H
    if identity not in H: return False
    # Closure
    for a in H:
        for b in H:
            if G_op(a, b) not in H: return False
    # Inverses (find for each a)
    for a in H:
        if not any(G_op(a, b) == identity for b in H):
            return False
    return True

# Subgroup of (Z/6Z, +): {0, 3}
import math
H = {0, 3}
op = lambda a, b: (a + b) % 6
print(is_subgroup(H, op, 0))   # True

# Lagrange: |H| divides |G|
print(6 % 2 == 0)   # True
```

## Applied

- **Cryptographic protocols**: based on cyclic subgroups of large
  finite groups.
- **Coding theory**: linear codes are subgroups of $(\mathbb{F}_2)^n$.
- **Group theory in physics**: classification of particle states.

## Check Your Understanding

:::widget type=numeric-input prompt="$|G| = 10$. Possible subgroup orders?" answer=4 explain="$1, 2, 5, 10$ — four divisors.":::

:::widget type=numeric-input prompt="A group of prime order $p$ has only trivial subgroups (orders $1$ and $p$). Such a group must be cyclic. So order-$7$ groups are all cyclic. (1 yes.)" answer=1 explain="Yes.":::

:::widget type=numeric-input prompt="By Lagrange, the order of any element divides the order of the group. In $S_4$ (order 24), can an element have order $5$? $5 \\nmid 24$." answer=0 explain="No.":::

:::widget type=numeric-input prompt="Index $[D_5 : C_5] = ?$" answer=2 explain="$10/5 = 2$.":::
