import type { Component } from "solid-js";

const StepRevealer: Component<Record<string, unknown>> = () => (
  <div class="widget-explain">StepRevealer — coming in a follow-up commit.</div>
);

export default StepRevealer;
