import type { Component } from "solid-js";

const FractionVisual: Component<Record<string, unknown>> = () => (
  <div class="widget-explain">FractionVisual — coming in a follow-up commit.</div>
);

export default FractionVisual;
